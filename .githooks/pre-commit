#!/bin/sh
# 커밋 직전 자동 검수 — 불합격이면 커밋 차단. 검사 내용: scripts/check.cjs
node "$(git rev-parse --show-toplevel)/scripts/check.cjs" || {
  echo ""
  echo "🚫 커밋 차단 — 검수 불합격. 급하면 git commit --no-verify (비권장)"
  exit 1
}
