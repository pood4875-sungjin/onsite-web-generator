# TEMPLATE_STRUCTURE.md — 템플릿 프리셋

> 제품소개/매뉴얼 페이지의 기본 섹션 구성(프리셋). 사용자는 이 위에서 추가/숨김/순서변경. ⬜ 작성 중.

## productIntro (기본 순서 초안 — 실물 검증 반영)
announce → gnb → hero → productLineup → feature → benefit → process
→ industry → integration → trust → stats → testimonial → logoWall → newsroom → ctaFinal → footer

## manual (초안)
gnb → breadcrumb → (toc) → prerequisite → step … → callout → specTable → troubleshooting → faq → ctaFinal → footer

## 규칙
- 프리셋 = 기본값. 안전하게 주되 자유롭게 조정 가능(섹션 조립형).
- 9월 MVP는 핵심 섹션만 프리셋에 포함.

## TODO
- [ ] `templates/productIntroTemplate.json` / `manualTemplate.json` 로 코드화
